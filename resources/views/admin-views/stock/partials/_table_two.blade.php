<div class="table-responsive datatable-custom">
    <table id="dataTable" class="table table-hover table-borderless table-thead-bordered table-nowrap table-align-middle card-table w-100 text-start">
        <thead class="thead-light thead-50 text-capitalize">
        <tr>
            <th class="border-0 min-w-120">{{ translate('SKU') }}</th>
            <th class="border-0 min-w-120">{{ translate('item') }}</th>
            <th class="border-0 min-w-120">{{ translate('variation') }}</th>
            <th class="border-0">{{ translate('qty') }}</th>
            <th class="border-0">{{ translate('unit_Price') }}</th>
            <th class="border-0 text-center">{{ translate('delete') }}</th>
        </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>
